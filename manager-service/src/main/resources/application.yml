eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZoon: http://localhost:8761/eureka
  instance:
    hostname: localhost
    
server:
  port: 8306
  
spring:
  application:
    name: manager-service